/** Marca TaskArchitect — assets em /public/branding (PNG com fundo transparente). */
export const BRANDING = {
  name: "TaskArchitect",
  /** Logo horizontal (ícone + wordmark) — sidebar, nav */
  horizontal: "/branding/taskarchitect-horizontal.png",
  /** Ícone acima do texto — login, hero compacto */
  stacked: "/branding/taskarchitect-stacked.png",
  /** Só o símbolo — favicon via app/icon, avatares */
  icon: "/branding/taskarchitect-icon.png",
} as const;
