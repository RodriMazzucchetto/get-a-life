// Design unificado: OS e Pitch agora vivem numa única aba (/os).
// As abas OS|PITCH foram removidas — pitches são geridos inline nos cards de pilar.
export function OsSectionNav() {
  return null;
}
